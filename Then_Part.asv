function Rn_output=Then_Part(pre,y_mf)

point_n=101;
Rn_output=zeros(point_n,point_n,point_n);
for i= 1:point_n     %x1
    for j=1:point_n   %x2
        
        w = pre(i,j);
        con = min(w,y_mf);
        Rn_output(i,j,:)=con;
    end
end
